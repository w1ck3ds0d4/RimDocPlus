# dependencies
node_modules

# build output
dist
src-tauri/target

# tauri generated schemas
src-tauri/gen/schemas

# editor
.vscode
.idea

# misc
.DS_Store
*.log

# generated fixtures from a local RimWorld install (machine-specific)
src/dev-data/scan.json
src/dev-data/session.json
src/dev-data/workshop.json

# The sidecar's build output. The published binary is produced by `dotnet publish`,
# not committed: a 13 MB executable does not belong in a git history.
sidecar/**/bin/
sidecar/**/obj/

# The published sidecar, copied in by `pnpm probe:stage`. Built, not committed.
src-tauri/binaries/
