{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Listening for a repair run's progress, and opening a Workshop page in the real browser. Everything else this app does goes through its own commands, which are not plugin calls and are not governed here.",
  "windows": ["main"],
  "permissions": ["core:event:default", "opener:default"]
}
